spring.application.name=FinalYearProject
server.port=${PORT:8080}

# Database - Using Cloud SQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql:///${DB_NAME}?cloudSqlInstance=${CLOUD_SQL_INSTANCE}&socketFactory=com.google.cloud.sql.mysql.SocketFactory
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# JPA
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jeevanthedeveloper22@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# PayPal
paypal.clientId=${PAYPAL_CLIENT_ID}
paypal.secret=${PAYPAL_SECRET}
paypal.url=https://api-m.sandbox.paypal.com

# Cloudinary
cloudinary.cloud-name=dlbvlapnw
cloudinary.api-key=776156753981623
cloudinary.api-secret=${CLOUDINARY_SECRET}

# Kafka - We'll use Google Pub/Sub instead
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=chat-group

# File uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB